Update the showreel entry in the portfolio data to use the correct title.

The first `FEATURED` item on the work page currently reads "Veesually Showreel". It needs to be renamed to "Luxury watch editorial". The thumbnail (`luxury-watches-thumb-5s.jpg`) is already sourced from the 5-second mark of the video, so only the title string in `src/data/site.ts` needs to change.